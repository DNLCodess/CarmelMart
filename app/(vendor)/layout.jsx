// Vendor portal root layout — intentionally no Navbar or Footer.
// The vendor dashboard ships its own sidebar + header.
export default function VendorPortalLayout({ children }) {
  return <>{children}</>;
}
