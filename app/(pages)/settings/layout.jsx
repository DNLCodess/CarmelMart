export const metadata = {
  title: "Account Settings — CarmelMart",
  description: "Manage your CarmelMart account — update your profile, addresses, notification preferences, and security settings.",
};

export default function SettingsLayout({ children }) {
  return children;
}
