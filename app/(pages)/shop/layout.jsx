export const metadata = {
  title: "Shop",
  description: "Browse thousands of products from verified Nigerian vendors on CarmelMart.",
};

export default function ShopLayout({ children }) {
  return children;
}
