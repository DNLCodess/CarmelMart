export const metadata = {
  title: "Help & FAQ — CarmelMart",
  description: "Get answers to common questions about shopping, payments, delivery, and vendor registration on CarmelMart.",
};

export default function HelpLayout({ children }) {
  return children;
}
